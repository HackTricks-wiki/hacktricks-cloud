# Secret Manager — tested

Secret Manager. Covered (regional CMEK hijack, delayed-destroy TTL IR-defeat, regional enum blind spot).

## Replication repoint — VERIFIED LIVE as NOT possible (correctly excluded)
- `secrets.update --locations` to repoint replication → HTTP 400 "Updating secret replication is not
  supported" → replication is immutable post-create. Correctly NOT shipped.

## Standing UNVERIFIED candidate
- `secrets.rotate` / `enableManagedRotation` confused-deputy — hypothesised the rotation Pub/Sub +
  managed-rotation path could be abused as a confused deputy; **not verified, not published**.

## 2026-09-26 — regional Cloud SQL managed-rotation probe (inconclusive)
- [Google's setup guide](https://docs.cloud.google.com/secret-manager/regional-secrets/set-up-automatic-rotation-for-cloudsql-secrets-rs) says a regional `CLOUD_SQL_DB_CREDENTIALS` secret has a built-in identity that may receive `cloudsql.users.list` + `.update` on the project. The preview `EnableManagedRotation` request accepts an instance ID, username, and optional caller-chosen password. Hypothesis: a principal holding only `secretmanager.secrets.enableManagedRotation` on that secret can reset a Cloud SQL user's password through the secret identity, then use the chosen password to gain database privileges.
- Created a disposable `db-f1-micro` PostgreSQL instance, regional secret, custom SQL role with only the documented two SQL permissions bound to the secret identity, and a low-permission caller with only `secretmanager.secrets.enableManagedRotation` on the secret. The low caller's REST call passed the Secret Manager IAM check (`authorizationInfo` granted) but returned HTTP 403, `Permission denied on the Cloud SQL user or instance, or the resource may not exist.` The project owner received the same response using the same instance ID, `postgres` username, and request body. No Cloud SQL user update appeared in the audit trail. This does **not** establish whether the delegated permission works; the owner control failed too.
- Two earlier attempts ended before the rotation check because of a regional-secret gcloud create path mismatch and a transient Cloud SQL create operation. The CLI impersonation helper used a different disabled quota project; direct IAM Credentials calls with the lab quota project worked. Each run's SQL instance, secret, role, service account, and IAM binding was removed and verified absent.
- Next: use a separately created non-default PostgreSQL user, verify it exists after Cloud SQL's create operation completes, and establish a successful owner rotation control before testing the low-permission caller. Keep any genuine permission-boundary failure local for responsible disclosure; publish only expected behavior.

## 2026-09-26 — named-user control still fails (not a finding)
- The named-user control confirmed `htrotuser` existed before calling `EnableManagedRotation`. The owner control still returned 403 with the same Cloud SQL permission/resource message for `INSTANCE`, `PROJECT:INSTANCE`, and `REGION:INSTANCE`; `PROJECT:REGION:INSTANCE` returned 400 and an error showing the service had prefixed the project ID internally. In every case, the Secret Manager audit entry showed `secretmanager.secrets.enableManagedRotation` granted to the owner; no Cloud SQL user-update audit event appeared. The test instance, both regional secrets, both built-in-principal SQL bindings, test service account, and custom roles were deleted and verified absent.
- The gcloud help example uses `PROJECT:INSTANCE`, while the four-part error suggests an internal project prefix. That is a request-format clue, not proof of a valid target format or a security issue. Next diagnostic: give the **control secret's** built-in identity `roles/cloudsql.admin` on the disposable project to determine whether the documented two-permission custom role is the blocker; keep the limited caller's secret identity on the documented role. Do not publish until an owner control works.

## 2026-09-26 — successful owner control and delegated call (confirmation in progress)
- With a verified named PostgreSQL user and a **control secret identity** holding `roles/cloudsql.admin`, an owner `EnableManagedRotation` request succeeded (HTTP 200) when `instanceId` was the bare instance name. The `REGION:INSTANCE` and `PROJECT:INSTANCE` forms returned 403 in this setup.
- A separate caller had only `secretmanager.secrets.enableManagedRotation` on its regional secret, no Cloud SQL role. Its request returned 403 while the secret identity had only the guide's `cloudsql.users.list/update` custom role; after adding `roles/cloudsql.admin` to that secret identity, the *same caller* and request returned HTTP 200 with an enabled secret version. The secret payload is JSON with `instanceId`, `username`, and `password` fields. The first comparison wrongly compared the entire JSON string to the chosen password; the resulting `CHOSEN_PASSWORD_MATCH false` output does **not** indicate a backend mismatch. A repeat is running with the field parsed correctly and progressive role grants to identify the true prerequisite.
- The first round's SQL instance, both regional secrets, built-in-principal bindings, caller service account, and custom roles were removed. Independent instance/secret/SA/role/IAM checks found no residue. No zero-day or book technique has been claimed from this intermediate result.

## 2026-09-26 — audit visibility / stealth classification (documentation review)
- Rechecked the existing live-test audit observations against Google's [Secret Manager audit logging](https://docs.cloud.google.com/secret-manager/docs/audit-logging) and [Data Access configuration](https://docs.cloud.google.com/logging/docs/audit/configure-data-access) references. `AccessSecretVersion` is `DATA_READ` and is not logged by default; `AddSecretVersion`, `DestroySecretVersion`, `DisableSecretVersion`, `DeleteSecret`, `UpdateSecret`, and `SetIamPolicy` are `ADMIN_WRITE` and always recorded. No lab resource was needed or created for this classification.
- Added an explicit stealth rating to both privesc and post-exploitation Secret Manager techniques. Direct read is high stealth under the default audit configuration; destructive writes and IAM self-grants are low; generic metadata/alias updates are medium because the method is shared with ordinary changes but `updateMask` exposes the affected field. The regional CMEK technique and persistence techniques already had ratings.
- No new attack primitive found in this pass. Next audit target: apply the same per-technique visibility review to another service page; do not infer stealth only from a generic method name when a request field or resource state exposes the change.
